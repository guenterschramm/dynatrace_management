resource "dynatrace_metric_metadata" "_invalid_Value_68" {
  description = "1 if time correction is enabled, otherwise 0."
  metric_id   = "metric-erlang_vm_time_correction"
  unit        = "Unspecified"
}
