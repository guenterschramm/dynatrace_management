resource "dynatrace_metric_metadata" "_invalid_Value_182" {
  description = "The total amount of buffer memory that is not being used (either unallocated or in the free list)."
  metric_id   = "metric-kafka.producer.buffer_available_bytes"
  unit        = "Unspecified"
}
