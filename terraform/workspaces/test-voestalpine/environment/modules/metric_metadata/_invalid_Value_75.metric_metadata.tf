resource "dynatrace_metric_metadata" "_invalid_Value_75" {
  description = "The number of async threads in the async thread pool used for asynchronous driver calls."
  metric_id   = "metric-erlang_vm_thread_pool_size"
  unit        = "Unspecified"
}
