resource "dynatrace_metric_metadata" "_invalid_Value_16" {
  description = "The number of network operations (reads or writes) on all connections per second"
  metric_id   = "metric-kafka.producer.network_io_rate"
  unit        = "Unspecified"
}
