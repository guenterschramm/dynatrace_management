resource "dynatrace_metric_metadata" "_invalid_Value_166" {
  description = "The average per-second number of record sends that are dropped due to buffer exhaustion"
  metric_id   = "metric-kafka.producer.buffer_exhausted_rate"
  unit        = "Unspecified"
}
