resource "dynatrace_metric_metadata" "_invalid_Value_39" {
  description = "Number of processors available to the Java virtual machine."
  metric_id   = "metric-jvm.cpu.count.gauge"
  unit        = "{cpu}"
}
